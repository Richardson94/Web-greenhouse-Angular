export class Producto {
  constructor(
    public id: string,
    public name: string,
    public productionTime: string,
    public tempMax: string,
    public tempMin: string,
    public image: string
  ) {}
}
